# Properties defining the 2FAS-KW plugin.

plugin.id = io.github.yasakawa.faskw.authn.graphicalmatrix
plugin.version = ${project.version}
plugin.license = /io/github/yasakawa/faskw/plugin/LICENSE.txt

# The 2FAS-KW plugin depends on the standard Password and MFA modules.
plugin.modules.required = idp.authn.Password,idp.authn.MFA

# Compatibility metadata URL placeholder.
# Replace this with the production HTTPS metadata URL when the plugin is published.
plugin.url.0 = ${plugin.metadata.url}
