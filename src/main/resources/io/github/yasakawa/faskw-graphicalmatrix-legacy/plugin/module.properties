# Properties defining the GraphicalMatrix MFA module.

# Class to Module ID mapping.
io.github.yasakawa.faskw.graphicalmatrix.plugin.GraphicalMatrixModule = idp.authn.GraphicalMatrix

# Module owner.
idp.authn.GraphicalMatrix.plugin = io.github.yasakawa.faskw.idp.plugin.authn.graphicalmatrix

idp.authn.GraphicalMatrix.name = GraphicalMatrix MFA
idp.authn.GraphicalMatrix.desc = GraphicalMatrix MFA external authentication flow, self-service change screens, and optional management API.
idp.authn.GraphicalMatrix.url = /GraphicalMatrixConfiguration

# Initial Plugin packaging keeps environment-specific files outside the JAR.
# Installer/config scripts will copy templates into conf/graphicalmatrix and edit-webapp.
